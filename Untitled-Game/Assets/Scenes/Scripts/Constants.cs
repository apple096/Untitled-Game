using System.Collections;
using System.Collections.Generic;
using UnityEngine;
public enum State {
        Harvesting,
        Combating,
        Overworld
    }
public class Constants {
    public enum State {
        Harvesting,
        Combating,
        Overworld
    }
}